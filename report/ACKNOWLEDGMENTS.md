# Acknowledgments

Draft for the report section (after Conclusions, ~half a page, not counted in the 5 pages).
Fill in, then move it into the Overleaf. Delete this note before submitting.

**Report text** — written by us. No generative AI used for the writing.
*(if true: we only used <tool> to re-read the draft and get feedback; we wrote and checked everything ourselves.)*

**Code** — generative AI tools we used while developing:

| Tool (version) | Publisher | Used for |
|---|---|---|
| ... | ... | ... |
| ... | ... | ... |

We designed the experiments, chose the hyperparameters and ranges, and ran the trainings ourselves;
all generated code, numbers and figures were checked by us (plots come straight from the run logs).
*(or, if none: no generative AI tools were used for the code.)*

---

LaTeX (paste after `\section{Conclusions}`):

```latex
\section{Acknowledgments}
The text of this report was written by the authors; no generative AI tools were used for the writing.
% optional, if true:
% We used <tool> only to review drafts and get feedback; all text was written and checked by us.
We used <tool (version), publisher> while developing the code, for <short purpose>.
All experiments, hyperparameter and range choices, and training runs were done by the authors,
who reviewed and validated all generated code, results, and figures.
% or: No generative AI tools were used for the development of the submitted project code.
```
